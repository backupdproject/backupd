# EPIC R conformance matrix

Every gate line declares, as a cell: what it certifies, where it is asserted, and
the planted violation that has to make it red. One row per line of the spec's two
exit gates, one per planted violation the spec names that no gate line already
carries, with nothing dropped for not being ready yet.

`docs/EPIC-R-rename-backupd-to-retnd.md` is the contract. This file is the account
of which parts of it are checked by something that has been watched to fail, which
parts are checked by nothing, and which issue owns each gap.

## Why this file exists in this shape

A rename is the change most likely to produce a matrix that is entirely green and
entirely worthless, because the obvious check — "does the old name still appear?" —
passes trivially the moment somebody runs search and replace, and says nothing
about whether the product still works. Three checks in this repository were found
in a single day that could not fail, and this epic adds a guard whose naive form
has **fifty** false positives on the tree it ships with (`BackupDetailPage`,
`BackupDefaultsPage`, `BackupDomainPolicy` and six more). A guard that noisy gets
deleted, and a matrix that certifies it is worse than no matrix.

So every row carries a **falsification**: the specific mutation that must turn
that cell red. A row whose falsification has been run and watched fail is `PASS`.
A row whose falsification cannot be run yet, because the code it would mutate does
not exist, is `BLOCKED`, with the issue that unblocks it. There is no third
reading, and in particular there is no row that is green because nobody looked.

Three rows carry a **positive control** as well as a falsification, and they are the
three that matter most. R1.1's control is a file containing every lookalike the guard
must not catch: without it, "the guard is green" is indistinguishable from "the
guard matches nothing". R1.9's control is that a genuinely fresh install still
first-runs: without it, "it adopted the legacy path" is indistinguishable from a
product that can no longer be installed. R2.17's control is a deliberately failing
check being *blocked* by branch protection on the freshly transferred repository:
without it, "branch protection restored" is a tick nobody can tell from a lie,
because a required-check list is silently empty on a new repository.

Two rows, R2.4 and R2.14, are `BLOCKED` today like every other row and can never be
better than `PARTIAL` once their work lands, and that is the honest outcome rather
than a gap waiting to close. R2.4's store screenshots need real hardware, a real
installation and a real SFTP source, and §10 of the checklist forbids substituting a
mock for any of them. R2.14's subject is artwork: no text guard can read text that
has become an SVG `<path>` or read a raster at all — the guard's own `-I` flag
guarantees the second. So that row automates what can be automated (a manifest held
to the filesystem, a string scan of SVG `<text>`/`<title>`/`<desc>`) and names a
human acceptance step for the rest. Calling a human step `PASS` is how "unverified"
becomes "certified".

## How to read an outcome

| Outcome | Means |
| --- | --- |
| `PASS` | The check exists, runs in `scripts/ci-local.sh`, and has been shown to go red against a real planted violation. The falsification column names the mutation and it is automated in `scripts/rename/selftest.sh` or `scripts/compat/selftest.sh`. |
| `PARTIAL` | Part of the claim is checked and shown to fire, and the rest is not: either it waits on an unlanded issue, or it cannot be run here at all. The row says which half is which and which of those two it is. |
| `BLOCKED` | The check is specified here and cannot conclude today, because the code it certifies is not merged. Not a pass, and not a fail. The row names the issue. |

`BLOCKED` is a declaration, and it is checked rather than trusted. The four
structural tests in `core/tests/compat/matrix_test.go` were bound to a single
hard-coded pair of paths — `docs/EPIC-E-alternative-storage.md` and
`docs/conformance/epic-e-matrix.md` — so nothing checked *this* file at all;
#887 replaced those two constants with a table of (spec, matrix) pairs and added
this one, and the tests now run as a subtest per pair. Each of them is watched to
fail against this file specifically, not only against EPIC E's: the landing PR
plants a row citing a path that does not exist, a `BLOCKED` row citing a closed
issue, a ticked spec checkbox whose row is not `PASS`, and a dropped row, against
a copy of this file, and records each red.

What they hold: `TestTheMatrixDoesNotCiteSuitesThatDoNotExist` fails if a `PASS`
or `PARTIAL` row cites a path the repository does not have, and
`TestEveryBlockedRowCitesAnIssueThatIsStillOpen` resolves every issue a `BLOCKED`
row names against GitHub. `TestTheMatrixHasARowPerGateLine` compares the row ids
against the spec's own exit-gate checkbox lists, and
`TestTheSpecsExitGateBoxesAgreeWithTheMatrix` holds those checkboxes to these
outcomes in both directions, so a box cannot be ticked by hand and a row cannot be
dropped to tidy the table. `TestTheViolationLedgerIsHeldToTheSpecsGuardTable`
counts the `V.n` rows against §4 of the spec: this ledger carries only the guards
no gate row above already covers, so it is held to being contiguous from 1 and
never longer than the spec's own guard table, rather than to EPIC E's stricter
one-row-per-guard promise.

Two numbers that guard against a silently empty parser: this file's ids are
`R1.n`, `R2.n` and `V.n`, and EPIC E's are `P1.n`, `P2.n` and `Vn`, so a regexp
written for one matches nothing in the other and would read exactly as clean. The
tests fail on a zero-row parse for that reason.

Every row in this file but R1.1 is `BLOCKED`, which is correct: the spec and the
old-name guard are what have landed. R1.1 is `PARTIAL` rather than `PASS`, and the
row says which half is which.

## Where the checks live

| Thing | Path |
| --- | --- |
| The old-name guard and its mutation self-test | `scripts/rename/check-brand-drift.sh`, `scripts/rename/selftest.sh` |
| The CLI, config and upgrade surface corpus | `core/tests/compat` |
| The FR-38 adoption warning, the two-journal refusal and the fresh-install control | `core/tests/compat` (new cells `20-legacy-state-adoption`, `21-fresh-install-first-run`, `22-two-journals-refusal`) |
| Compose start from an unedited pre-rename file | `distribution/compose` |
| Hook environment, executed rather than inspected | `core/internal/workflow` |
| Metric names and the gauges-only rule | `core/internal/metrics` |
| Cookie read-compat and re-issue | `apps/common/auth/local`, `apps/common/csrf` |
| The installer's mount/unit/config migration | `scripts/install/test_install_docker_host.py` |
| The org-wide deep grep and its allowlist | `scripts/rename/check-brand-drift.sh` over both checkouts; the token enumeration in `docs/EPIC-R-rename-inventory.md` |
| The brand-asset manifest and the SVG string scan | `docs/design/brand-assets.md` and its filesystem test; the `<text>`/`<title>`/`<desc>` scan in the guard's gate step |
| The cutover checklist and rollback note | the R2.5 issue body; `.github/workflows/release.yml` for the identity assertions |
| The planted violations, automated | `scripts/rename/selftest.sh`, `scripts/compat/selftest.sh` |
| The gate step that runs them | `scripts/ci-local.sh`, `.github/workflows/ci.yml` |

---

## Phase 1 — the product renames itself

| # | Outcome | Certifies | Where | Falsification |
| --- | --- | --- | --- | --- |
| R1.1 | PARTIAL (#887 landed; the org and first-brand reds are list-blocked until #895) | The old-name guard fires on a new old-brand identifier in each of the seven spellings FR-40 names, and does **not** fire on the domain word `backup` or on the nine `BackupD[a-zA-Z]` identifiers that already exist. | `scripts/rename/check-brand-drift.sh` (eleven patterns, green in under two seconds with 141 `pending` tokens and 154 pinned `preexisting` pairs); `scripts/rename/selftest.sh`, 26 cases in throwaway repositories | Seven reds, all watched: files containing `backupd_newthing`, `BACKUPD_NEW_THING`, `BackupdWidget`, `RCLONE_MANAGER_NEW_THING`, `backup_manager_state`, a new `https://github.com/backupdproject/…` link and the prose `rclone-manager`; each must name file and line. **Positive control, and the load-bearing half:** one green over a file containing `BackupDetailPage`, `BackupDefaultsPage`, `BackupDomainPolicy`, `BackupDetail`, `TestBackupDataAreSeparateMounts`, `TestBackupDataOnEveryClaimedPlatform`, `BackupDomain`, `TestBackupDoesNotReturnWhileAWorkerIsStillReading`, `TestBackupDoesNotLeaveItRunningForever`, `BackupSet`, `backup-set`, `BACKUP_DIR`, `backup_status` and rclone's `ibm_signer.go`. Without that control a guard matching nothing is indistinguishable from a guard that works. **Which half is not done, and why it is not waiting on code:** `backupdproject` and `rclone-manager` are on `pending` today, because FR-40 puts the repository coordinates and the in-flight brand words there, and a `pending` entry is allowed anywhere by design — so those two reds are watched against a list with that entry deleted, which is the state #895 and #889 leave the tree in, and until then the guard does not refuse a new link to the old organisation in *this* tree. Stated rather than ticked. |
| R1.2 | PARTIAL (#888, #889, #890 landed; every surviving `pending` token is a Phase 2 sweep, list-blocked until #891, #892, #893, #894 and #895) | No `pending` entry remains for anything Phase 1 owns: the module path, the binaries, the runtime identifiers, the deployment identity. | `scripts/rename/check-brand-drift.sh`'s own stale-entry report, quoted in each landing PR | The guard reports a list entry that matches nothing and does **not** fail on it, by design, so the falsification is the reverse: re-adding one deleted occurrence must produce a red, and deleting a `pending` entry whose occurrences still exist must also produce a red. Both run in `selftest.sh`. |
| R1.3 | PASS (#888 landed) | Every path-shaped tool resolves `github.com/retnd/retnd`, the `retnd` organisation name is reserved, and nothing in the tree instructs anybody to `go install` or `go get` the module while its fetch location still differs. | `go build ./...` and `go vet ./...` in all five modules; `scripts/architecture/check-layer-manifest.sh`, `check-core-dependency-rule.sh`, `check-layer-ownership.sh`, `check-unowned-go.sh`, `scripts/architecture/selftest.sh` (41 controls) and `verify-core-without-distribution.sh` / `verify-core-without-apps.sh`, each run before and after the sweep and compared; `scripts/docs/package-doc.baseline` regenerated (2 keys moved); `.golangci.yml` and `scripts/architecture/layers.conf` carry no `cmd/backupd` pattern, which running the checks is what established; `scripts/rename/check-no-module-fetch-instruction.sh`, the new scan, registered in `scripts/ci-local.sh` and `.github/workflows/ci.yml` | The failure mode here is a check that silently stops matching anything rather than one that goes red, so each path-shaped check gets a mutation that must be caught: a file placed in a layer it is not allowed in must still be refused **after** the sweep. For the `go install` half: a doc line reading `go install github.com/retnd/retnd/core/cmd/retnd@latest` must turn the scan red **before the cutover**, and the check is deleted by #895's cutover PR because afterwards the instruction is true. Both ran and both were watched. The layer plant: a `core/service` file importing `github.com/retnd/retnd/distribution/packaging`, with the module wired in so it resolves, is refused post-sweep with "core is in the core layer and imports github.com/retnd/retnd/distribution/packaging, which is in the distribution layer"; removed, and the check is green again. The `go install` plant: one line in `docs/install.md` naming the new path, and one in `docs/deployment.md` naming the old one, each turned the scan red and each was reverted. The org-name reservation is not a test, it is a precondition the PR records: `gh api orgs/retnd` answers for an organisation created 2026-09-13, and after this sweep 2,273 occurrences name it. |
| R1.4 | PASS (#888 landed) | `retnd --help`'s usage block, every verb in the dispatch table and every row of `docs/site/reference.html` agree; the compat corpus's CLI cells differ from their previous capture in the name and nothing else. | `distribution/packaging/site_reference_test.go`, `TestUsage_EveryRegisteredCommandIsPinned` and `TestUsage_NamesEveryBackupSetVerb` in `core/cmd/retnd`, cell `06b-cli-usage-block` in `core/tests/compat` | Both watched. A verb renamed in the dispatch table only (`reconcile` to `reconcilio`, `core/cmd/retnd/main.go` and nothing else) turned `site_reference_test.go` red in both directions at once -- one command omitted from the page, one named that the binary does not register -- and was reverted. For the corpus half: a variant that also reordered the usage block (swapping `run` and `daemon`) turned cell `06b-cli-usage-block` red on lines it already held, under the cell's may-only-grow rule, which is the check that a regeneration cannot launder an unrelated change through this rename; reverted. The rename's own re-capture moved 44 of the corpus's 2,877 lines and every one of them is the previous line with `backupd` read as `retnd`: no line was added, removed or reordered, and no `/etc/backupd` path moved. |
| R1.5 | PASS (#890 landed) | A container built from this tree starts from an **unedited** pre-rename compose file, through `/backupd-web`, and from the new one through `/retnd-web`. | `distribution/compose` contract tests, run twice against the same image | An image built without the `/backupd-web` hardlink must fail the unedited-compose run with `exec /backupd-web: no such file or directory`, which is the exact message `container/compose.yaml`'s header warns about. The control is that the new path is not merely a copy: a build where `/retnd-web` is absent must fail the other run. |
| R1.6 | PASS (#889 landed) | A hook script reading `$BACKUPD_BACKUP_STATUS` and one reading `$RETND_BACKUP_STATUS` observe identical values. | `core/internal/workflowexec/hookcompat_test.go`, asserting through **executed** scripts rather than the exported map | A build exporting only `RETND_*` must fail. Asserting on the map would pass against a build that exports the legacy name and then unsets it, and the whole point of this row is what a Bash script sees, so the assertion is a script that prints the variable and a comparison of its stdout. Falsified for real: deleting the compat block from `Environment.Resolve` made the hook print `status=` where the current name printed `status=failed`. |
| R1.7 | PASS (#889 landed) | `retnd_*` and `backupd_*` gauges are both scraped with identical values, the legacy `# HELP` names its replacement, and no counter is duplicated. | `core/internal/metrics/compat_test.go`, with the name pins in `metrics_test.go` and `snapshot_test.go` | Two reds, both exercised. A build emitting only `retnd_*` fails the name pin. **And the control that keeps the rule honest:** relaxing `duplicateUnderLegacyPrefix`'s `# TYPE` test to copy counters made five `backupd_workflow_*_total` families appear in the compat block and failed the run, because a duplicated counter double-counts under any aggregation and the gauges-only rule is the reason this window is safe at all. The counter families are fed through the duplicator itself rather than only through `Render`, which emits no counter today and would therefore have passed the plant. |
| R1.8 | PASS (#889 landed) | A session cookie minted before the upgrade authenticates after it, and is re-issued under `retnd_session`. | `apps/common/auth/local` (`handler_test.go`, `authenticator_test.go`), `apps/common/csrf`, and the cookie declarations in `api/v1/openapi.json` | A build rejecting `backupd_session` must fail the survival test, and does: reducing `sessionCookieNames` to the current name turned the handler and the webhost authenticator seam to 401/unauthenticated for both deprecated names. The re-issue half is asserted separately, because a build that accepts the old name forever and never re-issues passes the survival test and never closes the window — which is how `bm_session` outlived its own deprecation. |
| R1.9 | PASS (#890 landed) | All four cells of FR-38's table: state at `/var/lib/backupd` with nothing at `/var/lib/retnd` is adopted, served and warned about on every start and creates no administrator account and no enrollment token; two different populated directories are refused with both named; one host directory mounted at both container paths is **not** refused; a genuinely empty deployment still first-runs. | New compat cells `20-legacy-state-adoption` (pinning the warning), `22-two-journals-refusal` (pinning the refusal) and `21-fresh-install-first-run` in `core/tests/compat` | **The epic's most important row.** The planted violation is a build that takes the first-run path when the legacy directory holds a database; cell 20 must go red. **Two controls, and both are load-bearing:** cell 21 asserts a genuinely empty deployment still first-runs, so "it adopted" cannot be satisfied by a product that can no longer be installed; cell 22's control drops the same-device-and-inode test and requires the installer's own rollback-window compose file — one host directory mounted at both paths — to stop starting, so "it refused the ambiguous case" cannot be satisfied by a product that refuses whenever both paths are populated. A fourth assertion checks the negative directly: after the adoption, no administrator row and no enrollment token were created. |
| R1.10 | PARTIAL (#888, #889, #890 landed: the regenerated bindings pass both contract checks and the release gate covers every job; `ci-local.sh` is red on an unrelated EPIC E contract gate, re-run at #895) | `scripts/ci-local.sh` green; `check-contract-drift.sh` and `check-client-paths.sh` pass with the regenerated bindings; the release gate's `needs` list covers every job. | `scripts/ci-local.sh`, `scripts/api/check-contract-drift.sh`, `scripts/api/check-client-paths.sh`, `scripts/tests/release-gate-covers-every-job.test.sh` | A hand-edited `core/apicontract/contract.gen.go` that does not match `api/v1/openapi.json` must turn drift red; a new CI job left out of the release gate's `needs` must turn the release-gate test red. Both already fire today and are re-run after the sweep, because a path-pattern change is exactly how a check stops seeing its own subject. |

## Phase 2 — everything a user, a store reviewer or a contributor reads

| # | Outcome | Certifies | Where | Falsification |
| --- | --- | --- | --- | --- |
| R2.1 | BLOCKED (#895) | `check-brand-drift.sh` is green with an **empty `pending` list**, and every remaining occurrence of the old name is on `aliases` with its closing issue or on `preexisting` with its written reason. | `scripts/rename/check-brand-drift.sh`, `scripts/rename/selftest.sh` | Moving one surviving occurrence off its list must turn the guard red, and adding a `preexisting` token to a file the list does not name must turn it red too — the path half of that list is what makes "the occurrences that exist stay green, the same name in a new file is a creation" true rather than aspirational. |
| R2.2 | BLOCKED (#891) | All eleven providers' packaging manifests are regenerated through `distribution/packaging`'s derivation rather than hand-edited; the cross-provider conformance suite passes. | `distribution/packaging/matrix_test.go`, `matrix_guards_test.go`, `derive_test.go`, `conformance_test.go`; `apps/common/tests` | A hand-edited `canonical.json` that the derivation would not produce must turn the derivation test red. The control: a provider removed from the matrix must also turn it red, so "all eleven" is counted rather than assumed. |
| R2.3 | BLOCKED (#892, #893) | `reference.html`'s command table matches the dispatch table; all five site pages, their `<title>`s and the wordmark say `retnd`; 44 screens are re-recorded through the four capture scripts against `createMockApi` with the clock pinned. | `distribution/packaging/site_reference_test.go`; `docs/site/tools/capture-*.mjs`; the landing PR names the `tests-repo.pin` sha the Playwright was borrowed at | A verb added to the dispatch table and not to the page must turn `site_reference_test.go` red. The capture half cannot be falsified by a test — a picture is not an assertion — so its evidence is procedural and stated as such: the clock is pinned (`page.clock.setFixedTime`), so a re-record with no UI change produces a byte-identical file, and a diff that shows movement in a screen the epic did not touch is the signal. A hand-taken image is refused on sight. |
| R2.4 | BLOCKED (#893) | Every store-listing row whose screenshot shows the product name is **outstanding**, not passing, and `docs/submission/screenshots.md` says why it cannot be satisfied here. | `docs/conformance/submission-preflight.md`, `docs/submission/screenshots.md` | Nothing has landed, so this row is `BLOCKED` on #893; once it has, it can never be better than `PARTIAL` and will stay there: store screenshots require real hardware, a real installation and a real SFTP source, and substituting a mock there is forbidden by `docs/epic-checklist.md` §10. The checkable half is the **direction**: a row marked passing while its screenshot still shows the old name is the failure, so the falsification is flipping one such row to passing and requiring the preflight report to refuse it. |
| R2.5 | BLOCKED (#893) | The site's "What has not been proven" section states which surfaces were re-captured, which store screenshots still show the old name, and that no provider store listing has been re-reviewed. | `docs/site/index.html#honest` | Ungated by construction — it is prose about what was not done, and no test can know that. Its falsification is the honest one: if R2.4 stays `PARTIAL` and this section does not say so, the phase exit gate is not met, and the reviewer of the landing PR is the check. Stated here rather than dressed up as automation. |
| R2.6 | PARTIAL (#894 landed; `ui/shared`'s eslint step and four of its vitest cases were already red on `main` and are not this rename's) | Nothing the browser shows an operator says `Backupd`: the 199 display-name occurrences are `retnd`, the API interface and the thrown error type are `RetndApi` and `RetndError`, and the `backupd` command lines the UI echoes beside a wizard step are `retnd`; `tooltips.json` has no entry nothing names and every entry's copy still uses only `<strong>`, `<em>`, `<code>` and `<br>`; `fieldHelpCopy.ts` has the same 59 entries with all three parts each; both npm scopes are `@retnd/*` and both workspaces install and pass; the nine `BackupD[a-zA-Z]` identifiers are unchanged. | `ui/shared/src/test/registry-tooltips.test.tsx` (the orphan sweep, the allowed-tag sweep, and the #829 preference asserted with the preference explicitly ON), `tooltip-opt-out.test.tsx`, `tooltip-auto-attach.test.tsx`, `tooltip-trigger-click.test.tsx`, `tooltip-overflow-portal.test.tsx`, `wizard-field-help.test.tsx`; `ui/shared/src/test/protected-lookalikes.test.ts` (the nine, named one at a time) and `ui/shared/src/test/npm-scope.test.ts`; `npm run lint`, `npm run typecheck:providers`, `npm test` and `npm run build` in `ui/shared`, plus `npm run lint`, `npm run eslint` and `npm test` in `apps/common/tests`; `scripts/rename/check-brand-drift.sh` with its stale-entry report and `scripts/rename/selftest.sh` | Both falsifications run and watched. Renaming `BackupDetailPage` to `RetndDetailPage` turned `protected-lookalikes.test.ts` red twice over — as a failed `?raw` import when the file moved with the identifier, and as six failed assertions when only the identifier moved — and was reverted, with all five `BackupD*` occurrence counts equal to the pre-sweep tree afterwards. A `package-lock.json` left on `@backupd/ui-shared` turned `npm-scope.test.ts` red, and that check had to be written because the acceptance's assumption was false: `npm ci` installs that lockfile and exits 0, since both packages are `private`, nothing depends on either by name, and `@backupd/` reduces to a token still on the guard's `pending` list. `PARTIAL` rather than `PASS` because the row claims the whole frontend gate and `ui/shared` carries 6 eslint errors and 4 vitest failures on `main` — byte-identical before and after this issue, in `StepLogTerminal`, `vendored-fonts` (`WorkflowRunPage.tsx` asks for weight 800 and no face is shipped at it), `backup-set-inline-edit`, `backup-set-remove` and `poll-interval-per-set` — none of which this rename touches. The registry half's asymmetry is unchanged: an entry nothing names is red, a control with no entry is not, and that is §5 of the checklist rather than this issue's. |
| R2.7 | BLOCKED (#895) | `backupd-tests` suites pass against a stamped build of this tree; `tests-repo.pin` and their `build-under-test.json` name each other; nightly-e2e is green. | `scripts/e2e/tests-repo.pin`, `scripts/bdtools/e2e/run_tests_repo_gate.py`, `.github/workflows/nightly-e2e.yml`, and `build-under-test.json` in `backupdproject/backupd-tests` | A pin bumped on one side only must turn the gate red, and that is the falsification worth running because it is the way this pair has gone wrong before: the pin's own ledger records a bump whose other half "still has to move by hand". The suites' own pass is not evidence of this row; the lockstep is. |
| R2.8 | BLOCKED (#895) | `NOTICE`, the licence inventory and `provenance/**` are regenerated forward, and no already-published record is rewritten. | The licence-inventory gate; `provenance/{checksums.txt,release-provenance.json,sbom.spdx.json,third-party-licenses.json}` | A regeneration that rewrites an existing `release-provenance.json` entry must be refused: the falsification is a diff touching a published record, and the check is that `provenance/**` grows and never changes a line it already has. |
| R2.9 | BLOCKED (#895) | FR-42 holds four ways: an unedited pinned compose file starts, works and warns; the new compose file finds its state at the new path; two different populated directories are refused while one mounted twice is not; an installer upgrade migrates mounts, units and persisted config and shows only new names. | `distribution/compose`, cells `20-legacy-state-adoption`, `21-fresh-install-first-run` and `22-two-journals-refusal` in `core/tests/compat`, `scripts/install/test_install_docker_host.py` | The build that first-runs when the legacy path holds a database fails the first. For the installer half: a migration that moves the mount without rewriting `config.yaml`'s absolute `state.database` path must fail, because that combination produces a deployment whose config names a path that no longer exists — the failure the installer exists to prevent. A half-migrated host with both systemd units enabled must be refused, not tolerated. |
| R2.10 | BLOCKED (#892) | The CHANGELOG `[Unreleased]` entry names the issue, what changed, why, and what an existing deployment sees, including the one-release windows and the metric double-count caveat. | `CHANGELOG.md` | Ungated. Its falsification is a reader's: an entry that does not let an operator predict what their deployment does on upgrade has not met the line, and `docs/epic-checklist.md` §9 is the standard. Named here so it is reviewed rather than assumed. |
| R2.11 | BLOCKED (#895) | This file has no `BLOCKED` row, and every `PASS` row's falsification has been run and watched to fail. | `core/tests/compat`'s matrix tests: `TestTheMatrixHasARowPerGateLine`, `TestTheMatrixDoesNotCiteSuitesThatDoNotExist`, `TestEveryBlockedRowCitesAnIssueThatIsStillOpen`, `TestTheSpecsExitGateBoxesAgreeWithTheMatrix` — **generalised from EPIC-E's hard-coded `specPath`/`matrixPath` to a table of (spec, matrix) pairs by #887, so this file is one of the pairs they run over.** What is left for this row is the outcomes themselves: no `BLOCKED` row may remain | Editing a row's outcome from `BLOCKED` to `PASS` without the check existing must turn the citation test red; ticking a spec checkbox whose row is not `PASS` must turn the agreement test red. Both directions, because this file's predecessor sat with seven rows claiming too little long after the work landed and nothing in the repository could tell. |
| R2.12 | BLOCKED (#895) | The deprecation-window-close issue is filed, naming every `aliases` list entry it deletes, with the removal release from FR-43's table. | The filed issue itself, with `scripts/rename/check-brand-drift.sh`'s `aliases` list as its ledger | An `aliases` entry carrying no closing issue and no removal release must be refused by the guard, which is what stops this row from being satisfied by an issue naming fewer shims than exist: the ledger is the list, not the issue body. An undated shim is not hypothetical — `RM_DEBUG` reached its third rename without one. |
| R2.13 | BLOCKED (#895) | The org-wide deep grep across **both** repositories for `backupd|backupdproject|rclone[-_ ]manager|backup[-_ ]manager|RCLONE_MANAGER_|RM_[A-Z]|BM_[A-Z]|bm_|rbm_` returns only the enumerated allowlist: immutable history plus the six dated compat shims of FR-43. | `scripts/rename/check-brand-drift.sh` run over both checkouts; the allowlist derives from `docs/EPIC-R-rename-inventory.md` | Two directions, both required. Deleting one allowlist entry must go red, and adding one occurrence outside the allowlist must go red. The second direction is the one that matters: a scan whose allowlist has grown to cover whatever was found is a scan that certifies nothing, so an `aliases` entry carrying no removal release and no closing issue must also be refused. |
| R2.14 | BLOCKED (#893) | No brand asset **depicts** the old name: the wordmark, logo mark, favicons and app icons at every declared size, the eleven store icons, the `docs/design/` mockup art and the 44 site captures all read `retnd`. | `docs/design/brand-assets.md` (the manifest, held to the filesystem in both directions); the SVG `<text>`/`<title>`/`<desc>` scan in the guard's gate step; and a **named human acceptance step** with the manifest in front of the reviewer | **This is the row for the class automation structurally cannot cover.** `check-brand-drift.sh` greps tracked source: it cannot read text that has become an SVG `<path>`, and its own `-I` flag means a raster is never read at all. So the falsifications cover only what can be falsified — an asset in the manifest that does not exist, and an asset on disk with no row, each turn the manifest test red; a planted `<title>backupd</title>` (which `docs/site/assets/icon.svg` carries today) turns the scan red. The remaining half is a human looking at every asset in the manifest and signing the acceptance line. It is `BLOCKED` until #893 lands and `PARTIAL` rather than `PASS` forever after that, because calling a human step `PASS` is how "unverified" becomes "certified". |
| R2.15 | BLOCKED (#895) | The cutover happened **after** everything in-tree was green: `retnd/retnd` and `retnd/retnd-tests` exist, old paths redirect for web and git, every `#N` still resolves, and a clone of an old URL still works. | GitHub's own transfer behaviour, exercised and recorded in the cutover PR: a clone of the old URL, a fetch of an old `#N`, and the `go.mod` path now matching its fetch location | The ordering claim is the falsifiable part: the cutover PR must be unable to merge while any Phase-1 or Phase-2 in-tree gate is red, which is a required-check dependency rather than a convention. The redirect claim is observed rather than asserted — the PR records the three probes — because "GitHub redirects it" is exactly the sentence nobody checks. |
| R2.16 | BLOCKED (#895) | The four references a redirect does not cover are re-established: the cosign/OIDC identity re-issued for `retnd/retnd` with the documented `verify` command presenting the old identity for pre-cutover releases and the new one after; the four `raw.githubusercontent.com` URLs republished and every occurrence updated; `ghcr.io/retnd/retnd` publishing with `ghcr.io/backupdproject/backupd` mirrored for one release from the retained organisation; the Pages origin moved and the old one documented as dead. | `.github/workflows/release.yml` and its publish/verify assertions; the four URL occurrences; the GHCR mirror workflow in the retained organisation; `.github/workflows/pages.yml` | Four falsifications, one per reference, and each has to be run because each fails differently. A release signed under the new identity must **fail** verification against the old one and pass against the new; a `verify` command in the docs naming only one identity must fail the release gate; a raw URL left at the old path must fail the link check; a release run pushing only `ghcr.io/retnd/retnd` with no mirror must fail the publish gate. The Pages half is observable rather than assertable: the old origin returns 404, which the PR records. |
| R2.17 | BLOCKED (#895) | The org-level carry-over is complete: profile and avatar, teams and members, org and repository secrets and variables, GHCR package ownership and visibility, Actions settings, branch protection with its required-check list, Pages source and custom domain, the label set, and both repository descriptions and topics saying `retnd`. | The R2.5 checklist, item by item, with the rollback note | **None of this is in a diff, which is the whole hazard.** The falsification is the one that catches an empty checklist: a required-check list is silently empty on a freshly transferred repository, so the cutover PR must record a *deliberately failing* check being blocked by branch protection on the new repository. Without that, "branch protection restored" is a tick nobody can distinguish from a lie. The description half has a control already in the tree: `backupd-tests`'s description still says `rclone-manager`, two renames later, and if it still says it after this row the row is false. |
| R2.18 | BLOCKED (#895) | No surviving reference to the `backupdproject` organisation or to the old repository names anywhere in either tree, outside a redirect-backed link deliberately left as history. | `check-brand-drift.sh`'s `backupdproject` pattern (added in #887), run over both checkouts | A new `https://github.com/backupdproject/…` link must turn the guard red. The control is the deliberate exception: a link left as history must be on the `preexisting` list with its path, so that the same URL appearing in a new file is still a creation. Distinct from R2.13 because that row's allowlist is about the product's name and this one is about the organisation's — and the organisation survived the last rename by not being in anybody's pattern. |

## Rows with no gate line of their own

These come from the spec's section 4 planted-violation table and the FR bullets
that name a mutation of their own, and are not restatements of a gate line above.
They are here rather than folded into a neighbour because each one is a distinct
mutation with a distinct owner.

| # | Outcome | Certifies | Where | Falsification |
| --- | --- | --- | --- | --- |
| V.1 | PASS (#890 landed) | The config-directory half of FR-38: a resolved configuration directory with no `config.yaml` beside a legacy directory holding one is an adoption, not a fresh install. | New compat cell `20-legacy-state-adoption`, second case | A build that treats the missing `config.yaml` as a fresh install must turn the cell red. Separate from R1.9 because the two directories are resolved by different code and an implementation can easily guard one and not the other — and the configuration directory is the one that also holds the SSH key store and `known_hosts.d/`. |
| V.2 | PASS (#889 landed) | The deprecation warning for a legacy input variable is emitted once per name per process start, not per read. | `core/envcompat/envcompat_test.go`, and `core/internal/obs/envlevel_test.go` for the reader | A build warning on every read must fail, and does: removing the once-per-name guard emitted ten notices for ten reads of two names. A per-read warning on `BACKUPD_DEBUG` in a hot path is a log flood, and a log flood is how a deprecation notice gets filtered out by the operator it was written for. |
| V.3 | PASS (#888 landed) | `legacyName` in `apps/generic/cmd/retnd-web/selfname_test.go` is `backupd`, and that test still fails on a binary that calls itself by the previous name. | `apps/generic/cmd/retnd-web/selfname_test.go` | Watched in #933: a string literal `backupd-web usage:` printed from the web host's own `usage()` turned `TestNothingPrintsANameThisBinaryDoesNotHave` red (`main.go:213 spells "backupd" outside a path segment`) and `TestUsageIntroducesThisBinaryByName` with it, and the plant was reverted. The test exists because it caught this once already at `rbm`; moving the constant without re-proving it fires would leave the third rename with the guard the second one had to have restored by hand. Moving `legacyName` to `backupd` also required the rule underneath it to grow a right-hand anchor -- `_` continues a runtime identifier (R1.4's cookies and metric series) and `-` followed by anything but `web` is a deployment name (R1.5's) -- plus three path-pinned mail subjects owned by R2.2, each held to the tree by `TestTheExemptionsAreStillReal` so a pin cannot outlive what it excuses. |
| V.4 | PASS (#890 landed) | The `ghcr.io/backupdproject/backupd` mirror publishes for one release, so an unedited compose file keeps pulling. | `.github/workflows/release.yml`, the publish step's own assertions | A release run that pushes only the new package name must fail the publish gate. Paired with R1.5: the entrypoint alias and the image mirror are two independent halves of "an unedited compose file still works", and a build can satisfy either alone. |
| V.5 | PASS (#890 landed) | The adopted path is reported through the surfaces that report every other resolved path, so an operator can see which one is live without relying on a warning they scrolled past. | `retnd check`, the deployment-check route in `apps/common/webhost`, the startup log; cell `20-legacy-state-adoption` | A build that adopts the legacy path and reports the new one must turn the cell red. This is the row that stops adoption from being a silent success: a deployment can run for months on an adopted path, and "which directory is my journal in" is an incident question. |
| V.6 | BLOCKED (#895) | No new config key exists: a config written by this build parses byte-identically under the previous build. | `core/tests/compat`, the older-build parse cell | A variant that adds a `legacy_paths:` key must be refused by the previous build under `KnownFields(true)`. That refusal **is** the proof the key was never safe, which is why the mutation is kept rather than described: it is the only way to show that the compiled-in-constant decision was not merely convenient. |
